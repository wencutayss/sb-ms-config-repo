server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/order_schema?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
